#include <iostream>
#include <iomanip>
#include <cmath>
using namespace std;
float f(float x, float e) {
    int i = 1;
    float pi;
    int b;
    int k = -1;
    pi = M_PI;
    float c = -(pi) / 2;
    b = 1;
    while (c > e) {
        c += (1 / -b*(pow(x, i)));
        b += 2*k;
        k *= -1;
        i += 2;
    }
    return c;
    
}
int main()
{
 float e;
cin >> e;
    for (float i = -3; i <= -2; i += 0.1) {
        cout << i << " " << f(i, e) << endl;
}
}
