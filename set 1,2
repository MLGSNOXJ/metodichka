#include <iostream> // для работы с вводом и выводом
#include <vector> // для работы с векторами
#include <set> // для работы с множествами

using namespace std;

int main() {
    int n; // переменная для хранения количества чисел
    cin >> n; // считываем количество чисел

    vector<int> numbers(n); // создаем вектор для хранения чисел
    for (int i = 0; i < n; i++) {
        cin >> numbers[i]; // считываем числа и сохраняем их в векторе
    }

    set<int> digits; // создаем множество для хранения цифр
    for (auto num : numbers) { // перебираем числа из вектора
        while (num > 0) { // пока число не равно нулю
            digits.insert(num % 10); // добавляем последнюю цифру числа в множество
            num /= 10; // удаляем последнюю цифру числа
        }
    }

    set<int> missing_digits; // создаем множество для хранения отсутствующих цифр
    for (int i = 0; i <= 9; i++) { // перебираем все цифры от 0 до 9
        if (digits.find(i) == digits.end()) { // если цифры нет в множестве digits
            missing_digits.insert(i); // добавляем ее в множество отсутствующих цифр
        }
    }

    if (missing_digits.empty()) { // если множество отсутствующих цифр пусто
        cout << "There are no missing digits." << endl; // выводим сообщение об отсутствии отсутствующих цифр
    } else { // иначе
        cout << "Missing digits: "; // выводим сообщение о наличии отсутствующих цифр
        for (auto digit : missing_digits) { // перебираем отсутствующие цифры
            cout << digit << " "; // выводим каждую отсутствующую цифру
        }
        cout << endl; // переходим на следующую строку
    }

    return 0; // завершаем программу
}

#include <iostream> // для ввода/вывода через консоль
#include <string> // для работы со строками
#include <set> // для использования множества

using namespace std;

int main() {
    int n; // количество строк, которые нужно обработать
    cin >> n; // ввод количества строк

    // Обработка каждой строки
    while (n--) {
        string s; // строка для обработки
        getline(cin, s); // ввод строки с пробелами
        set<char> unique_letters; // множество уникальных символов
        set<char> repeated_letters; // множество повторяющихся символов

        // Поиск уникальных и повторяющихся символов
        for (char c : s) { // перебор каждого символа в строке
            if (isalpha(c)) { // проверка, является ли символ буквой
                if (unique_letters.count(c)) { // если символ уже встречался в строке
                    repeated_letters.insert(c); // добавляем его к множеству повторяющихся символов
                } else { // если символ встречается впервые
                    unique_letters.insert(c); // добавляем его к множеству уникальных символов
                }
            }
        }

        // Вывод уникальных символов
        for (char c : s) { // перебор каждого символа в строке
            if (isalpha(c) && !repeated_letters.count(c)) { // если символ является буквой и не повторяется в строке
                cout << c; // выводим символ
            }
        }
        cout << endl; // выводим символ переноса строки для следующей строки
    }
    return 0; // завершение программы
}


